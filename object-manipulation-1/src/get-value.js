/* exported getValue */
function getValue(object, key) {
  var value = object[key];
  return value;
}

// define a function named getValue which takes two parameters, object and key"
// define a variable value and set it to the object parameter at index key parameter
// return the value variable
