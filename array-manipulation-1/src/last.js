/* exported last */
function last(array) {
  var lastElement = array[array.length - 1];
  return lastElement;
}

// define a function named "last" which takes a single parameter "array"
// define a variable "lastElement" which contains the last element of the array
// return the value of "lastElement"
