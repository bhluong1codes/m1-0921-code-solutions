/* exported head */
function head(array) {
  var firstElement = array[0];
  return firstElement;
}

// define a function named "head" which takes a single parameter "array"
// define a variable "firstElement" which contains the first element of the array
// return the value of "firstElement"
