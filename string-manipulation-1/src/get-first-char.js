/* exported getFirstChar */
function getFirstChar(string) {
  var firstChar = string.charAt(0);
  return firstChar;
}

// define a function named getFirstChar which takes a single parameter "string"
// define a variable "firstChar" which will contain the first character of "string" parameter
// return value of "firstChar" variable
