/* exported getLastChar */
function getLastChar(string) {
  var lastChar = string.charAt(string.length - 1);
  return lastChar;
}

// define a function named getLastChar which takes a single parameter "string"
// define a variable "lastChar" which will contain the last character of the "string" parameter
// return the "lastChar" variable
